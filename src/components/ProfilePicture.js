import React from 'react'

// CRUFT: unused stub, never imported anywhere. The actual top-left profile
// picture is already handled by the MUI <Avatar src="/ilsaProfPic.png"> in
// Header.js. Safe to delete unless this was meant to become something more
// than an <Avatar> (e.g. an editable/uploadable picture for Jane's Guild
// member pages — in which case rebuild it rather than resurrecting this stub).
// this will be a square image in the top left of web page.
export default function ProfilePicture() {
  return (
    <div>ProfilePicture</div>
  )
}
