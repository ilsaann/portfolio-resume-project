'use client';
import React from 'react'

// STUB: placeholder shell, not imported anywhere yet - real intake fields
// are specced in the comments below but not built.
export default function NewMember() {
  return (
    <div>NewMember</div>
  )
}

// this is going to be an intake for new guild members,
// we need their title, lets give some (eg. helper text suggests: artist, crafter, photographer, yogi, witch, etc) under but reg Text Field
// we want an about that takes a couple sentances introducing member + work
// we want contact links: linkedIn, Facebook, Instagram
// optional location tag for where their creative offerings are

// but with edit switch
// with edit mode editable sections are given a gold highlight
// we want save all changes button and or autosave if not too costly
//      when pushed it toggles edit switch off

// shall we render a skeleton of the connoisseur view of their 'About Me' page, that is just in place editable with the switch?
// yes

// we will have them toggle themes in the menu
// mode switch in menu too
